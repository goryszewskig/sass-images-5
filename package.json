{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:sandbox": "echo 'Use: npm run dev for local development'",
    "build": "vite build",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "deploy:prod": "echo 'Production deployment disabled - development only'",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:create": "wrangler d1 create webapp-production",
    "db:migrate:local": "echo 'DB commands disabled - using local development database'",
    "db:migrate:prod": "echo 'Production database removed'",
    "db:seed": "echo 'DB commands disabled - using local development database'",
    "db:reset": "echo 'DB commands disabled - using local development database'",
    "db:console:local": "echo 'DB commands disabled - using local development database'",
    "db:console:prod": "echo 'Production database removed'",
    "clean-port": "fuser -k 3000/tcp 2>/dev/null || true",
    "test": "curl http://localhost:3000"
  },
  "dependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "hono": "^4.9.2",
    "jsonwebtoken": "^9.0.2",
    "stripe": "^18.4.0"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
